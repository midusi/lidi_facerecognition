numpy
opencv-python
pyqt5

scikit-learn
scikit-image
scikit-video

# sudo apt install ffmpeg
ffmpeg

# for speech synthesis, install espeak
# sudo apt install libespeak1 libespeak-dev
pyttsx3

progressbar2

# for the web server
requests

# face_recognition needs dlib installed _before_ running pip install -r requirements.txt
# See https://github.com/ageitgey/face_recognition
# https://www.pyimagesearch.com/2018/01/22/install-dlib-easy-complete-guide/
# install dlib
#sudo apt-get update
#sudo apt-get install build-essential cmake libopenblas-dev liblapack-dev  python python-dev python-pip python3 python3-dev python3-pip libx11-dev libgtk-3-dev
# activate venv
# pip install dlib

# For example with: https://www.learnopencv.com/install-dlib-on-ubuntu/
face_recognition
